# Findings: PR-008 Review Follow-up

## 2026-08-27 PR-MVP-EVAL-005 Authority and Design Boundary

- The owner authorized only a non-formal offline A/B diagnostic over the already frozen MVP universe.
- The existing `HistoricalBacktestEngine` can preserve the unchanged strategy kernel if entry eligibility is injected at the engine boundary; institutional fields do not need to enter `GapVwapEntryStrategy`.
- Both arms require the same context-bar stream so prior-session close and intraday cumulative VWAP are identical. The price-only predicate admits audit-qualified symbols on the exact 60 target sessions; the institutional predicate admits only frozen `(target_session, symbol)` memberships.
- `HistoricalDatasetCatalog.iter_bars_ordered()` validates the immutable full Dataset while streaming. A read-only filtered view can reuse it without creating a second Dataset.
- The approved Dataset uses `COMMON_LOTS` volume and `DERIVED_CLOSE_X_VOLUME_PROXY` amount semantics; these limitations must be pinned in the diagnostic plan and result.
- The formal protocol strategy/cost contract is reusable as a reference-only frozen identity, but this MVP run must not construct `CompositeResearchInputManifestV1` or claim formal holdout evidence.

## Review Requirements

- Verdict through line 500: `APPROVED WITH CONDITIONS (Foundation Approved)`.
- The framework is approved; the evidence gate remains pending and must not be represented as strategy success.
- Evaluation bounded context, five arms, composite manifest, time split, matched control, session-clustered CI, market/liquidity/turnover guardrails, and four-way holdout verdict were accepted.
- Holdout remains untouched: viewing results and then changing thresholds/re-running invalidates evidence.
- Missing `TEST_POSTGRES_DSN` is non-blocking for the foundation, but real PostgreSQL adapter verification is recommended before the untouched holdout.
- The first 500 review lines state there is currently no blocker; remaining conditions are in the unread tail.
- Complete review conditions before formal evidence execution:
  1. Freeze actual gate parameter values before holdout.
  2. Keep the sole primary hypothesis `COMBINED - PRICE_ONLY` net-expectancy difference.
  3. Freeze setup, entry, exit, holding-period, and cost-model definitions.
  4. Do not inspect holdout before definitions are frozen; never retune and rerun it.
  5. Persist negative/null outcomes (`FAIL`, `BLOCKED`, `INSUFFICIENT_EVIDENCE`) as evidence.
- Reviewer explicitly says: do not add features; freeze the evaluation protocol and run the first untouched holdout.
- Final gate state: PR-007 approved, PR-008 framework approved, PR-008 evidence pending, PR-009 on hold.

## Repository Findings

- Repository search found no owner-approved production gate values or `FormalEvaluationGateV1` artifact.
- Numeric values in `tests/test_institutional_formal_evaluation.py` are synthetic test fixtures (`2` sessions/executions, zero turnover allowance) and are not research approvals.
- Architecture explicitly delegates actual confidence level, sample size, and guardrail values to the research owner before holdout.
- No formal evaluation observation population or historical institutional holdout artifact exists under `research/`; only unrelated freshness and daily-Kbar qualification artifacts are present.
- The existing v0 domain already pins the primary metric, threshold digest, setup/outcome/cost definitions, split ranges, negative verdicts, and non-actionable flags. The missing work is actual protocol values plus evidence data, not another evaluator feature.
- Backtest runtime has frozen strategy IDs and cost defaults, but no owner decision selects which entry/exit family is authoritative for this evidence campaign.
- Repository default costs are commission `0.001425`, sell tax `0.003`, and slippage `5 bps`; these are existing runtime defaults, not automatically approved research protocol values.
- `data/backtest` currently exposes only `backtest.sqlite3`; no immutable historical bar dataset directory was found.
- An initial `sqlite3 -readonly` inventory failed to open the database, so dataset/run counts remain unverified until a non-writing immutable connection is attempted.
- Python SQLite immutable inspection succeeded: `backtest_datasets=0`, `backtest_runs=0`, and `backtest_trades=0`; there is no local evidence population to run.
- The existing price-only system's closest stable baseline is `legacy_gap_volume_vwap_entry_v1`; ORB, RSI/Bollinger, ATR stop, and time stop are marked experimental and should not silently replace the baseline.
- Candidate setup currently freezes a 2-4% gap, 100,000 cumulative shares, BuyScore 40, and close strictly above VWAP. Existing stable exits include 2% stop loss, 3% take profit, and end-of-day force close.
- Selecting the entry/exit family is still an owner decision because it determines the primary research question; repository defaults alone are insufficient authorization.
- Strategy definitions already expose deterministic `definition_digest` values and can be pinned directly in the protocol artifact.
- Existing research artifacts use human-readable JSON with explicit schema versions and issue/status fields; the new protocol should follow that pattern and use a sidecar digest rather than inventing a runtime service.
- `EvaluationThresholdsV0` exactly supports the approved numerical gate. A preregistration artifact can validate by constructing this existing domain object; no evaluator-domain version bump is needed.
- Approved threshold bundle deterministically produces thresholds digest `0ee3c4fe8ac247b07f3283e02f0b451cf75725143211ac760e008db50ef024a6`.
- Frozen strategy definition digests: entry `5ebf451d...eca61`; stop loss `dc99330d...fb46`; take profit `2532845a...9276`; EOD `a41ca995...a49e`.
- Existing PIT universe records already provide date-effective market, industry, market cap, and size cohort inputs needed for matched controls; ADV20 liquidity still requires prior-session price/volume data.
- Registration timestamp for the approved protocol is `2026-08-20T16:09:32+08:00`; exact split dates remain intentionally unresolved until coverage-only inspection.
- Frozen protocol canonical digest is `b769cf1b672e15d599cf610d22bda49d37981ddc36ae999b120b52702b197dc4`.
- The protocol explicitly keeps all outcome and holdout permissions false until a coverage-resolution artifact supplies exact ranges and the composite manifest is research-eligible.
- The acquisition plan defines coverage-only allowed fields, chronological split rounding, minimum holdout sessions, PIT arm construction, deterministic matched controls, and one-shot holdout preservation.
- Aggregate definition identities are now frozen for setup, outcome, cost model, and evaluation plan; exit priority is risk-first (`stop loss`, `take profit`, `EOD`) and execution is next-bar-open except EOD session-close.
- The frozen protocol permits only session date, market, source availability, PIT eligibility count, row count, and issue code during coverage resolution; price values and all setup/execution/return fields remain prohibited.
- The repository calendar artifact covers calendar year 2026 and is explicitly TWSE-scoped; it cannot prove TPEx calendar consistency or provide a multi-year formal-evaluation population by itself.
- The current coverage snapshot must preserve null coverage dates and null member counts rather than turning unavailable evidence into numeric zero; observed dataset/session counts may be zero only when the source inventory actually established zero.
- The first `PopulationCoverageV1` snapshot is correctly blocked by eight explicit issues; the only partially pinned input is the TWSE-only 2026 calendar, so TPEx calendar coverage remains unproven.
- Snapshot canonical digest: `4a4fee1101a425f4a04f2bee0204b26b2163cc022c302162b76b6a09195d76a2`.
- The repository has executable TWSE/TPEx institutional source adapters and reviewed test fixtures, but no durable production raw-artifact directory or validated partition set; adapter capability is not acquisition evidence.
- `data/premarket` contains unrelated premarket context/raw files and must not be counted as formal institutional, price, PIT-universe, corporate-action, or reference datasets.
- The only acquired dataset-like input eligible for the acquisition inventory is the TWSE-scoped 2026 calendar artifact; its TPEx applicability is unproven, so its acquisition status is `PARTIAL` rather than `ACQUIRED` or `VALIDATED`.
- The acquisition manifest must separate planned source metadata from acquired artifact identity so an endpoint or adapter cannot satisfy a dataset gate.
- The first `DatasetAcquisitionManifestV1` records five required datasets as `MISSING` and the TWSE-only 2026 calendar as `PARTIAL`; no dataset is promoted to `VALIDATED`.
- Acquisition manifest canonical digest: `75a9b6081648f4c4e3df74acef4ab07f9e87db06b726e3b9c728fd266f93501b`.
- The first official fetch sealed a TWSE 2026-08-19 raw artifact with SHA-256 `e341b1c629b7a543c9ed347346d3f7633ae44ed1a536002a51824912e920a381`; normalized publication was not attempted after the diagnostic-formatting failure.
- TWSE replay validated 1,336 normalized rows with 10,688 `PASS` checks and no issues; normalized digest is `0d52e2216ac07e67221de7e598de44149a80b0902e36fdec3e3c48e482543f5a`.
- The TPEx endpoint ignored the requested `20260819` date and returned `20260820` with 912 source rows. The raw response is preserved under digest `b1eac7a6807659f84e3365ef159a3b303fb0c56e3c6dc4d2b048ed0b0e87aeef`, but the partition is quarantined for `RESPONSE_DATE_MISMATCH` and `EMPTY_PARTITION`.
- The wrong-date TPEx response carried the current-day scope subtitle and was not usable to review the requested session. The corrected slash-date response for 2026-08-19 explicitly included ordinary, block, odd-lot, and omnibus-account investment-trust volume and matched the frozen scope contract.
- Official TPEx page behavior uses slash-form historical dates; the repository adapter's compact `YYYYMMDD` request was a real source-contract bug. The adapter now sends `YYYY/MM/DD` while still validating the response's compact date field.
- Corrected TPEx revision 2 validated 892 normalized rows with 7,136 `PASS` checks and no issues; normalized digest is `b3495fe7cffebea6c0231cbdee923d2cb3da2f9e5bf2c21fef35e509aa8d3eca`.
- The first two-market partition set contains 2,228 rows for 2026-08-19, has canonical digest `1e6e78317d6b3e46ca4011fc948ad42878121f06fbfff8ee3161e23062f551dd`, and is deliberately `VALIDATED_PARTIAL_COVERAGE`.
- Acquisition manifest r2 promotes only Institutional to `PARTIAL`, replaces the missing-partitions issue with `INSTITUTIONAL_HISTORY_INCOMPLETE`, and retains every downstream gate as false. Its canonical digest is `f23a220631b09a3a54c220558a66774f96ce6b0ef6ea6fca831f85415129cf12`.
- The acquired source identifiers include four-, five-, and six-character symbols; this is not treated as a source anomaly and reinforces that the PIT security-type filter is mandatory before research-arm construction.
- Completion-gate inventory finds reusable price-dataset and PIT-universe contracts/adapters, but no newly sealed formal artifact for either family under `research/institutional_evaluation/datasets`; executable capability still does not satisfy acquisition.
- `data/backtest/backtest.sqlite3` remains the only local backtest database and prior immutable inspection found zero datasets/runs/trades. Premarket contexts, test fixtures, and daily-Kbar qualification samples are explicitly outside the formal acquisition inventory.
- The repository has a reviewed TWSE-only 2026 calendar plus a TAIFEX calendar; TAIFEX is not TPEx and cannot close the required dual-equity-market calendar gate.
- Existing price contracts support immutable RAW daily derivation and source lineage, but the frozen intraday strategy also requires full intraday OHLCV/VWAP inputs. A daily-only artifact cannot promote the formal price family to `VALIDATED`.
- A fresh immutable SQLite inventory still shows zero sealed `backtest_datasets`, runs, results, trades, comparisons, decisions, and equity rows. It does show 542 history-partition rows and 6 jobs, so those staging records require coverage/status inspection before concluding that no recoverable price acquisition bytes exist.
- The 542 price staging partitions all belong to one `PAUSED` download job and contain 9,335,704 bars across 542 symbols; partition metadata spans varying start dates from 2023-08-21 through 2026-08-18 and end dates through 2026-08-18. Because start coverage differs by symbol and some partitions have zero bars, this staging set cannot be called a complete formal price dataset without finalization and coverage validation.
- Five other price jobs are failed or stale-running. No `backtest_datasets` manifest references the 111.7 MB SQLite staging store, so the bytes are recoverable acquisition evidence but not yet an immutable dataset artifact.
- The paused price job requested 2,738 current instruments. Of 542 saved partitions, 412 are non-empty and 130 carry the legacy `資料來源未回傳 Kbar` error; the downloader's resume contract deliberately retries this suspicious tail rather than finalizing it.
- `DatasetAcquisitionCompletionGateV1` now freezes the all-six-family rule. Current summary is 0 validated, 2 partial, and 4 missing; all downstream permissions remain false.
- Completion artifact canonical digest is `4226f096233a2637647b1d6373a4307ed05390f63a1b7636b7e1dae7ce4ac514`.
- The supported resume path is `PROVIDER=shioaji .venv/bin/python scripts/download_backtest_history.py --resume dataset-download-f914feaddea04e37b3cbdcfce2b0179b`; it reuses the same provider identity and database checkpoints rather than starting a new job.
- The downloader preserves completed partitions, retries the first legacy empty/error tail, limits Kbar calls below the documented provider threshold, and only seals `bars.jsonl` plus `manifest.json` after all requested instruments are resolved.
- Provider initialization requires the Shioaji optional SDK and API key/secret, logs in with `subscribe_trade=False`, and does not load a CA or enable orders. Resume preflight must check credential presence without printing values and must not run concurrently with another dataset job.
- Resume preflight passed: SDK and both credentials are present, provider is Shioaji in simulation mode, no external database override is configured, and `lsof` found no process holding the SQLite file. The only `RUNNING` database record is stale by days.
- The frozen expected acquisition universe contains 2,738 unique symbol/market identities with digest `2ad3e4880f6537390952f120032d8a3892435fc8d1586671cda63de6ef64708f`; staging contains no unexpected symbols and every recorded partition digest has the expected 64-character format.
- The downloader invalidates the durable tail from the first retry anchor, so the sealed resolution classes are 411 trusted success-prefix symbols, 131 checkpointed-but-unresolved tail symbols, and 2,196 not checkpointed, for 2,327 unresolved symbols total. The earlier raw 412/130 staging count is not the safe resume boundary.
- Repository-wide artifact scan found no production PIT snapshot/manifest outside test fixtures, no corporate-action artifact, and no reference-data artifact. The completion gate must keep those families `MISSING`.
- The approved external resume logged into Shioaji in simulation/data-only mode, retried symbol 1259, received another empty response, and exited safely with code 75. No partition or dataset manifest was written.
- `PriceAcquisitionResolutionV1` canonical digest is `6eb6cb4719d999ba49eec46c814383fbfb00b9cf1533f88b8837e9da1c41b5b9`; its expected identity digest is `2ad3e4880f6537390952f120032d8a3892435fc8d1586671cda63de6ef64708f` and staging metadata digest is `54f43181f9c5f71a1c9a9c6b8d4bddf040f808112a50d4f78e3b7a23cb4a5ac2`.
- The job targets `ALL_CURRENT`, so even a fully replayable acquisition remains survivorship-limited until it is reconciled against the date-effective PIT universe and delisted-security reference history.
- Focused gates passed with 32 tests; the full repository passed with 839 tests and 2 skips. Ruff remains not executed because it is unavailable.
- TPEx announcement `1000101804` proves 1259 began ordinary-board trading on 2011-12-15, before the job's 2023-08-19 start date; raw response digest is `1532842a4da62f99ff91ff68a35f50c29241274edc356593f2a8a553b93a02c7`.
- The exact-date TPEx historical daily report for 2026-08-18 contains one 1259 row and has response digest `4037715879ae9afa2441a2ddae6ff1cab444e09e945ccb3b5ab0c6e59cefadde`; price values were not retained in classification evidence.
- Shioaji currently resolves 1259 to `OTC`, `STK`, name `安心`, update date 2026-08-20. A bounded non-persisting 2026-08-01 through 2026-08-18 probe returned zero Kbars.
- Same-job TPEx controls immediately before 1259 are non-empty: 1240 has 9 bars and 12561 has 2 bars on 2026-08-18. This rejects a market-wide TPEx path failure but does not identify the provider-side root cause.
- Symbol 1259 is classified `SYMBOL_SPECIFIC_PROVIDER_COVERAGE_MISMATCH`, not `STRUCTURAL_NO_DATA`. It is neither retry-approved nor exclusion-approved until the provider route or an alternative intraday source is independently resolved.
- Symbol-classification artifact canonical digest is `3726f6b74ba7ca986f5c3537204336488e7c4fefa3c88b098a0f79f69a112293`.
- The technical report's six binary checks show five available evidence layers and one absent layer: 1259 bounded Shioaji Kbars. This visual is diagnostic availability, not a completeness rate or outcome metric.
- Focused classification gates passed with 38 tests; the full repository passed with 845 tests and 2 skips. Compile, digest, and whitespace gates also passed; Ruff remains unavailable.
- Shioaji 1.7.2 official documentation supports stock Kbars for `STK` with `TSE` or `OTC`, documents Stock history from 2020-03-02 to today, and returns timestamp/OHLC/Volume/Amount; no per-symbol or per-session completeness guarantee was identified.
- Shioaji explicitly warns that traffic exhaustion can produce empty market-data responses and says to inspect usage/meaning before retrying. That warning prevents an empty response from proving structural absence, but it does not explain the already isolated 1259-only mismatch.
- Fugle's current historical-candle contract is the strongest first qualification candidate: listed/OTC one-minute data starts 2023-05-23, regular-stock minute volume is in lots, and `average` is cumulative from market open. Actual entitlement, symbol 1259, PIT/delisted coverage, missing-session behavior, and cross-source equivalence remain unverified.
- FinMind documents one-minute listed/OTC Kbars from 2019 with lot units and historical all-market Sponsor Pro files, but the KBar schema has no cumulative average or amount. Exact frozen-strategy VWAP would require its separate tick route plus a reconciliation proof.
- TWSE documents over 15 years of historical all-market trade and quote products, but the public material does not establish a single dual-market, one-minute, VWAP-ready contract; it remains procurement discovery only.
- `PriceProviderCoverageResolutionV1` freezes twelve source-qualification criteria, an explicit no-silent-mixing rule, and a structural-exclusion evidence policy. No alternative is selected and all permissions remain false.
- Provider-coverage resolution canonical digest is `9beda1f6131a6fd7b384cfe77083f6f5c027eaec72c4e0d1155ac59f091a8418`.
- Focused provider-resolution/upstream gates passed with 19 tests. Full regression reached 859 passes and 2 skips with one unrelated concurrent golden-digest failure in `tests/test_live_entry_thesis_draft.py` (`PENDING_GOLDEN` versus computed `3ad4d65b...d857`).
- Fugle's current REST contract requires `X-API-KEY`; local `.env` has no Fugle- or FinMind-named entitlement key, so a credentialed source probe cannot be executed without new external authority.
- Fugle documents one-minute listed/OTC history from 2023-05-23, ISO 8601 timestamps with `+08:00`, minute `average` as cumulative from market open, and regular-stock minute volume in lots. These are documentation-level contract matches, not observed payload evidence for 1259 or PIT completeness.
- Fugle states no-data ranges return HTTP 404 rather than an empty array; a future credentialed probe must preserve HTTP status and response digest so `404` is not silently converted into a structural exclusion.
- Fugle minute `adjusted` is not supported; only daily/weekly/monthly adjustment is documented. The qualification contract must therefore freeze raw intraday prices and keep corporate actions as a separate artifact.
- Shioaji remains the control route: official docs support historical ticks/Kbars for backtesting but also warn that traffic/rate violations can produce empty responses or suspended access. A source comparison must record usage/HTTP/provider status, not only row counts.
- `IntradaySourceQualificationV1` freezes thirteen checks and returns `INSUFFICIENT_EVIDENCE`: 2 policy-level passes, 5 documentation-only insufficient checks, 5 not-executed checks, and 1 entitlement failure.
- The bounded credentialed probe is frozen to 1259, TPEx controls 1240/12561, TWSE controls 2330/2317, and official control session 2026-08-18. A five-session early/middle/late sample remains null until a validated dual-market calendar can select it.
- Response evidence must retain HTTP status, secret-free headers, source version, retrieval time, and raw SHA-256. Fugle HTTP 404 or a missing minute is an issue code, never an empty success, silent fill, or structural exclusion.
- Qualification artifact canonical digest is `a1b86a545934a69d0a5a6b126ec70d084abc1f0e18b4465ac54fcb174defe053`.
- Focused qualification/upstream gates passed with 26 tests; the full repository passed with 867 tests and 2 skips. Compile, canonical digest, and scoped whitespace checks passed.
- Intraday Source Qualification review returned `APPROVED`; no contract, artifact, or test correction is required. The only authorized next gate is the frozen credentialed probe.
- A fresh local inventory still finds no Fugle-named credential or provider integration. The repository contains only the qualification artifacts and documentation links, so no authorized HTTP request can be made yet.
- The user added `FUGLE_API_KEY`; a secret-safe recheck confirms the key exists and is non-empty. The value was not printed or persisted into research artifacts.
- The research owner approved the proposed VWAP reconciliation tolerance: `abs(source_average - reference_vwap) <= max(0.01 TWD, reference_vwap * 0.0001)`. It must be written to an immutable protocol and digested before any Fugle payload access.
- The pre-payload protocol is frozen under canonical digest `f6b396072d858356bcd98965ddafc749f2b8b63cfe3555f9f60f58a9c16d10f7`; its immutable registration predates the credentialed probe.
- The credentialed Fugle probe returned HTTP 200 for all fixed requests, but 1259 contained zero bars. Controls 1240, 12561, 2330, and 2317 contained 9, 2, 266, and 266 bars respectively.
- Fugle timestamps are start-minute labels while Shioaji controls are end-minute labels; adding one minute aligns regular bars, while both sources label the closing auction as 13:30. After alignment, all 543 control bars and every OHLC field matched.
- Frozen VWAP reconciliation passed for all four controls: absolute differences ranged from about 0.0032 to 0.0050 TWD and remained below each fixed tolerance.
- Fugle 2330 session volume was 17,209 lots versus Shioaji 17,217 lots; the eight-lot difference occurs in one aligned minute. The other three controls matched exact volume.
- Because 1259 remains empty and exact volume does not reconcile for every control, Fugle is `REJECTED_FOR_MISMATCH_RESOLUTION`, not selected for acquisition, and not evidence for structural exclusion.
- Probe result canonical digest is `b55d29b913bdee1f69b6a7e8a8982ca2e5bd66ebcb1584c8b9d4c32e59576710`.
- The approved review distinguishes three states: Fugle capability evidence is positive, Fugle as the 1259 mismatch resolver is rejected, and Fugle as a general price source remains unqualified rather than globally rejected.
- The next authorized gate is FinMind or official licensed-source qualification under the unchanged fixed probe and no-silent-mixing rule.
- Current official FinMind material identifies `TaiwanStockKBar` as Sponsor-tier, single-day one-minute data with date/minute/OHLC/volume fields, and `TaiwanStockPriceTick` as Backer-tier per-symbol single-day ticks with deal price, volume, time, and tick type.
- FinMind KBar has no documented cumulative average or amount field, so the frozen cumulative VWAP must be reconstructed from `TaiwanStockPriceTick` and reconciled against the KBar/control route; KBar alone is insufficient.
- FinMind's current OpenAPI documents bearer-token authentication and a lower unauthenticated rate limit, but dataset-tier authorization remains distinct from transport access. No FinMind-named local credential is present.
- The upstream `PriceProviderCoverageResolutionV1` already freezes `NO_SILENT_MIXING; PRESERVE_SOURCE_IDENTITY_PER_PARTITION_AND_FREEZE_PRECEDENCE`; Phase 15 should make the resulting dataset-revision consequence explicit rather than invent a parallel policy.
- The FinMind qualification artifact returns `INSUFFICIENT_EVIDENCE`: transport availability does not prove Sponsor KBar plus Backer Tick entitlement, and no local credential identity exists.
- Any future Shioaji-plus-secondary composition must receive a distinct dataset identity and revision; an existing partition may not be silently replaced in place.
- Alternative-source qualification canonical digest is `9a7111647292678c925c35a1aec6ff6afae1615b3e738f35aa7e20081a64f88b`.
- Phase 15 is complete as `INSUFFICIENT_EVIDENCE`; the next credentialed probe requires both a non-secret FinMind token identity and plan-level authority for KBar plus Tick before any payload access.
- `FINMIND_API_TOKEN` is now present and non-empty. Token presence proves only a local credential identity; actual KBar and Tick entitlement must be classified from the fixed authenticated responses.
- Current official FinMind OpenAPI specifies `GET https://api.finmindtrade.com/api/v4/data` with bearer authentication and query parameters `dataset`, `data_id`, and `start_date`; successful and rate-limit states are also represented inside the JSON status field.
- Current official catalog confirms plan ordering Free < Backer < Sponsor < Sponsor Pro, so Sponsor-or-higher should cover both Backer Tick and Sponsor KBar. Actual access must still be established by the sealed responses.
- Both `TaiwanStockKBar` and `TaiwanStockPriceTick` are single-day requests. KBar is minute OHLCV; Tick carries deal price, volume, Time, and TickType. The fixed 2026-08-18 session remains within the post-registration evidence scope and requires no date change.
- The FinMind-specific protocol was frozen before payload access with canonical digest `43aa0091e6c038da9714e847de0b8b6e93a80717640881a92efe2302567eb44e`.
- All ten fixed authenticated requests returned HTTP 400 and JSON status 400 without a data array. The common server envelope identified the account level as `register` and required an upgrade.
- Because access was denied uniformly for KBar and Tick across the target and four controls, no price value, minute alignment, volume unit, Tick-to-KBar reconstruction, or VWAP result could be evaluated.
- The sealed capture manifest digest is `4f477c513a0a5e5482d13bdc24d9280c01539c7e568c770c5feb4c161bfe36ed`; raw response hashes and the exact frozen request order were revalidated.
- The result remains `INSUFFICIENT_EVIDENCE`, not provider rejection: the credential exists, but the observed plan entitlement is insufficient. FinMind is not selected and 1259 remains neither resolved nor structurally excluded.
- The result artifact canonical digest is `4cb400713f7f5f32d71a346cddb175611ee32690dbd6a439271c990a202b6d46`.
- The research owner reports that the FinMind account has been upgraded to Sponsor. This authorizes a new immutable r2 bounded probe, but actual KBar/Tick entitlement must still be inferred only from authenticated r2 responses.
- The local `FINMIND_API_TOKEN` remains present and non-empty; its value was neither printed nor persisted during preflight.
- The existing capture implementation hard-codes r1 protocol/output identities. The smallest safe revision is to add explicit `--revision r1|r2` selection with r1 as the default, preserving old behavior while preventing r2 from overwriting r1.
- Sponsor probe r2 freezes the exact r1 request list and inherits all r1 timestamp, volume, VWAP, response-preservation, and decision rules by canonical reference. Its digest is `e0ecce5f893911d6a183396eee2fa94e2c785f3bb327c40882562d3d5e9e9f68`.
- The first r2 capture process was externally terminated before request 10 completed. Nine contiguous responses remain only in an unpublished staging directory; all nine are HTTP/JSON 200 with data arrays, which establishes Sponsor route access but not non-empty symbol coverage.
- Recovery must validate the staged prefix against the frozen request order and raw hashes, then request only the missing suffix. Repeating the first nine requests would violate the bounded single-probe intent and add avoidable provider calls.
- The original r2 process completed and atomically sealed all ten responses before recovery ran. The recovery attempt safely rejected the existing immutable output and issued no duplicate requests. Capture manifest digest is `edf2b5feb7d818dfa5802ff9010bd7d1abb3b432feb9e8bd70421fc76baa1a9c`.
- All ten r2 routes returned HTTP/JSON 200 with a data array, so observed dataset entitlement passes. However, 1259 and 12561 both have zero KBar and zero Tick rows; 1240 has 9/9, 2330 has 266/4,885, and 2317 has 266/8,967 KBar/Tick rows.
- FinMind Tick includes post-regular-session timestamps through 14:30 for TWSE controls. The frozen validator must restrict Tick evidence to 09:00 through 13:30 Asia/Taipei and must not treat post-session trades as VWAP inputs.
- Existing immutable Shioaji controls already contain Amount/OHLCV for 1240, 12561, 2330, and 2317. Reuse them rather than making any additional provider request.
- On available controls 1240, 2330, and 2317, FinMind KBar volume equals regular-session Tick volume exactly at 24, 17,217, and 47,879 raw units. Those same totals equal Shioaji lot volume, proving the fixed `RAW_VOLUME_IS_COMMON_LOTS_MULTIPLY_BY_1000` hypothesis for observed controls.
- Stable Tick aggregation by floor-minute reproduces every available FinMind KBar OHLCV exactly: 0 mismatches across 9, 266, and 266 bars. All TickType values are retained as frozen; no synthetic minute is inserted.
- Regular-session Tick VWAP is `56.933333...`, `2387.833246...`, and `250.464472...` for 1240, 2330, and 2317, exactly equal to the corresponding Shioaji Amount/volume VWAP values already frozen in the reference capture.
- FinMind 12561 is empty despite two Shioaji reference bars, while FinMind 1259 is also empty for both KBar and Tick. Therefore FinMind can demonstrate compatible semantics on available cross-market controls but cannot resolve 1259 and does not cover every fixed control.
- The immutable r2 result verdict is `REJECTED_FOR_MISMATCH_RESOLUTION`, canonical digest `4a259a737643b60c80193ea40076456d1deebc3aacd3be7bc310850d3d57f189`. Entitlement and available-control semantics pass, but target coverage and all-fixed-control availability fail.
- Focused r2/upstream gates pass with 37 tests. Full regression has 939 passes, 2 skips, and the unchanged unrelated operational-composition date/session fixture failure; excluding that file yields 933 passes and 2 skips.
- Full-token and Authorization-header scans are clean. Compile, canonical digests, and `git diff --check` pass; Ruff remains unavailable.
- The Sponsor r2 review approved the narrow rejection and formally exhausted Shioaji, Fugle, and FinMind as current public/API fallback candidates. The next authorized gate is `OFFICIAL_OR_LICENSED_INTRADAY_SOURCE_RESOLUTION_V1`.
- The new gate must treat API market support as insufficient evidence. Selection requires explicit historical coverage, PIT/delisted handling, minute semantics, corporate-action and correction policy, and license/retention rights.
- Current TWSE Data E-Shop documentation offers historical transaction files for all stocks from 2006-01-01, with internal-use restrictions and custom production, but explicitly excludes the most recent year. This is strong official lineage for older TWSE trades but cannot alone cover the frozen evaluation end period.
- TWSE distinguishes historical transaction/order/disclosure files from public post-close aggregates. Exact trade-file fields and aggregation semantics require the official format specification or a sample before one-minute/VWAP qualification.
- TPEx's official information shop lists historical transaction, order, and disclosure files and explicit usage pricing, establishing an official procurement route. The list page alone does not yet establish start date, recent-period availability, ordinary-equity scope, retention, or field semantics.
- TEJ/TQuant documentation explicitly covers TWSE, TPEx, and delisted securities with adjustment and daily attribute data, which is useful for PIT/reference/corporate-action support. The reviewed catalog section is daily frequency and is not evidence of historical one-minute data.
- TPEx's official transaction-file specification provides trade date, security code, trade type, trade time, trade sequence, price, and volume explicitly in shares. This is sufficient in principle to reconstruct regular-trading one-minute OHLCV and VWAP after qualifying trade-type/session filters.
- The TPEx product page states a 2022-11-01 start date, daily file production, historical ordering up to the previous month, and separately says older-than-one-year data requires a custom order. That recency wording is ambiguous enough that exact 2023-2026 availability must be confirmed contractually before selection.
- The TPEx transaction-file license page allows internal use and, under external-use terms, transmission to information users subject to subscription conditions. Repository retention, derived bars, and research reproducibility rights remain unproven until terms/order confirmation is obtained.
- TWSE publishes downloadable current transaction-file format documents, but the web reader cannot parse the DOCX/octet-stream files. A bounded local inspection of the official format file is required before claiming minute/VWAP field compatibility.
- The current TWSE transaction-file DOCX was downloaded only to `/tmp` and verified as SHA-256 `9791efc840f301a41148803cfd401bad99862eadeca464f631a39496a208282c`. It defines trade date, six-character security code, regular/block/odd-lot trade type, 12-character trade time, sequence, price, and volume in shares.
- TWSE's format change log states actual trade-time precision from the 2020-03-23 continuous-trading start, and the file contains post-close adjusted/reallocated trade records. These fields support one-minute OHLCV/VWAP reconstruction in principle under an explicit regular-trade filter.
- Search of current TEJ public product/API documentation found strong daily PIT, delisted-security, corporate-action, and stock-attribute coverage, but no primary documentation proving a historical one-minute equity product. TEJ therefore remains a supporting PIT/reference candidate, not the qualified intraday source.
- TWSE documents a signed information-vendor channel, including domestic and international firms, but being a signed vendor does not prove that any specific product provides retained, PIT-complete, dual-market historical trades for the frozen period.
- `OfficialOrLicensedIntradaySourceResolutionV1` records five documented semantic passes, one date-range failure, four contract-level insufficient-evidence checks, and three unexecuted acquisition/sample checks. Verdict is `INSUFFICIENT_EVIDENCE`; no source is selected.
- The next gate is intentionally procurement evidence, not provider coding: obtain written coverage/license/correction answers and a fixed five-symbol sample before any purchase or acquisition permission can be considered.
- Official/licensed source resolution canonical digest is `43cbe7621c6e70541a98e7d565d76ca0c56d8ece2c59ef6856cef4bfaccd921b`.
- Focused resolution/upstream gates pass with 29 tests. Full regression has 948 passes, 2 skips, and one unrelated operational-composition assertion failure; excluding that file yields 942 passes and 2 skips.
- Compile, canonical digest, and `git diff --check` pass. Ruff remains unavailable. No procurement, new provider payload, price outcome, or holdout access occurred.
- The research owner approved replacing the 100%-coverage search with a uniform pre-outcome data-coverage exclusion policy: PIT eligible AND institutional data available AND qualified intraday coverage complete. Symbol coverage must be at least 95% and session coverage at least 99%.
- This changes the evaluation population definition and therefore must be an immutable amendment referencing, not overwriting, the original frozen protocol. No outcome has been read, so the amendment remains methodologically admissible if completed before population freeze.
- The owner requires missingness-concentration audits across market, size, liquidity, industry, and delisted status. Exact concentration pass thresholds were not provided, so the first amendment must require reporting and owner review rather than inventing automatic thresholds.
- The original formal protocol digest remains `b769cf1b672e15d599cf610d22bda49d37981ddc36ae999b120b52702b197dc4`; outcome generation and holdout execution are still false, and the existing coverage snapshot digest remains unchanged.
- The existing population definition requires PIT ordinary equities in TWSE/TPEx and forbids survivorship substitution, but it does not define data-availability exclusions. The amendment can narrow the estimand to the fully covered subset without modifying strategy, outcome, cost, split, or hypothesis definitions.
- To make 95%/99% operational without a named-symbol exception: a symbol is coverage-eligible only if its complete PIT-eligible symbol-session ratio is at least 99%; at least 95% of PIT symbols must meet that requirement. Incomplete symbol-session rows remain excluded with `DATA_COVERAGE_EXCLUDED` and cannot be silently filled.
- The immutable coverage amendment digest is `6a5a943e720dc3bf0e9bc952805539a4c0a460d3f8137462f714773fbaba96ab`. It preserves the original protocol digest, freezes the 95% symbol gate plus per-symbol and aggregate 99% session gates, forbids named overrides, and narrows any eventual claim to the frozen data-covered PIT population.
- The initial data-coverage audit snapshot digest is `e7a7a98d4ea06e2a8bfdc17a2b1c96fe4a8b4d96c61cb18ef5e68d75dbe08382`. All measurements remain null, no exclusions are materialized, and population freeze/outcome/holdout remain blocked pending formal datasets and concentration review.
- Missingness concentration must report market, market-cap cohort, ADV20 liquidity cohort, industry, and active-versus-later-delisted groups. With no owner-approved numeric concentration threshold, the artifact requires a coverage-only owner review before population freeze rather than inventing an automatic pass rule.
- Focused coverage/protocol tests pass with 18 tests; the complete repository regression passes with 957 tests and 2 skips. Compile, JSON parse, canonical digest, `git diff --check`, and Ruff all pass.
- The existing resumable downloader treats an empty Kbar response as a recoverable pause and writes no partition. That default remains correct for ordinary acquisition, but it prevents a complete market-wide coverage scan after a known provider mismatch.
- Existing partition storage already supports zero-bar error observations for typed failures, and the dataset manifest remains `CURRENT_SNAPSHOT` plus `research_eligible=false`. A narrow explicit continuation policy can therefore record provider-empty coverage observations without treating them as successful data or formal evaluation exclusions.
- The current job still uses `ALL_CURRENT`, so even a completed scan cannot become the PIT evaluation universe. It can only provide provider-coverage evidence until a date-effective PIT universe and the other formal input families exist.
- The explicit coverage continuation preserves the default downloader pause contract. In coverage mode only, empty provider responses are persisted with `[PRICE_DATA_UNAVAILABLE] PROVIDER_EMPTY_KBAR`, while provider quota and temporary failures still pause with a retry symbol.
- `CompositeResearchInputManifestV1` now requires digest-bearing `formal_evaluation_protocol`, `coverage_amendment`, `coverage_audit`, and `frozen_population` identities; canonical serialization includes all four.
- The live data-only continuation authenticated against the Shioaji simulation environment and advanced past 1259. At the latest observed checkpoint it reached 428/2,738; the job remains resumable and cannot become research eligible because its universe is `ALL_CURRENT`.
- The active scan continued through at least 466/2,738 checkpoints. Additional zero-bar responses, including symbol 140201, are preserved as typed observations rather than being inferred as structural no-data or evaluation exclusions.
- `PriceCoverageScanConfigurationV1` pins Shioaji SDK `1.7.2`, the local adapter/download-runner/CLI SHA-256 identities, the immutable job request digest, and the 2023-08-19 to 2026-08-18 request range. Its metadata-only snapshot digest is `327ffefa42473e76fa50e3a51c57a3b1b20e8314c1a0a3c177d23f64a42f9de4`.
- The configuration snapshot is intentionally not a final result: it has 468 nonempty checkpoints, 2 typed provider-empty observations, and 72 legacy empty rows still requiring revalidation. `ALL_CURRENT` remains an acquisition target, not a formal PIT denominator.
- The preregistered PriceCoverageAuditV1 contract requires final protocol/amendment/scan/final-checkpoint/PIT/institutional/calendar/reference/corporate-action lineage. It requires symbol coverage, per-symbol and aggregate session coverage, concentration, and issue-reason distribution; only a final PIT audit may turn resolved provider empties into `DATA_COVERAGE_EXCLUDED`.
- Coverage scan taxonomy is now explicit: `PRICE_DATA_UNAVAILABLE` is a resolved provider-empty observation; `TEMPORARY_FETCH_FAILURE` and `SYMBOL_MAPPING_ERROR` are per-symbol observations for the later resolution phase; `RATE_LIMITED` is a whole-job pause and no partial partition is written. None is a formal evaluation exclusion before the final PIT audit.
- The active provider process was launched before the timeout/mapping revision, so its active configuration remains immutable. A later resume must pin a new scan configuration revision before using the new runner behavior.
- The live r1 coverage scan reached a typed whole-job safe-pause at 678/2,738 because Shioaji retained only 15.5 MiB of its 500 MiB historical-query allowance. The provider runner wrote no partial current partition. This is an immutable r1 boundary, not a final coverage result.
- The research owner approved segmented lineage: trusted pre-scan non-empty checkpoints are not rerun; legacy ambiguous empty observations remain `PENDING_REVALIDATION`; r1 and every later resume must be represented by distinct configuration-pinned segment manifests. Final raw-summary evidence must aggregate segment manifests rather than silently treating all checkpoints as one configuration.
- The r1 resume reobserved the former legacy-empty tail before the 678/2,738 safe-pause boundary. Its sealed metadata-only segment contains 259 `NON_EMPTY_SUCCESS` and 8 `PRICE_DATA_UNAVAILABLE` observations; no legacy-empty row remains pending inside that sealed r1 interval. The original r0 prefix remains separately sealed at 411 trusted non-empty checkpoints.
- `PriceCoverageScanConfigurationV1 r2` is frozen before any next provider resume. It pins the new timeout/mapping taxonomy, starts at original request index 678 (`2101`), requires the explicit coverage-scan option, and requires every later r2 observation to be sealed in a distinct segment before a raw summary can use it.
- The r0, r1, and r2 artifact digests are `53c42a21c170973933d4ac12555bf17fbe906136a9f0259fead59156cd7a17c2`, `30d609a22d6827a6bca47fefba4668ef681df053ce0f53dd432c84940b52df26`, and `d60502f51897bdf4492717ec49f07b52b09c5f7f60b8c5764d10b4295dc22797`. These are data-acquisition lineage only, not a formal coverage audit or research population freeze.
- The job remains durably paused at 678/2,738 with retry symbol `2101`; r2's artifact digest and both source hashes still match its frozen configuration. Shioaji has not reset its historical-query allowance, so no immediate retry is justified.
- An active current-task heartbeat will check in the next Taiwan weekday reset window. It is constrained to metadata-only preflight, the frozen explicit r2 command, and segment/config sealing after any subsequent rate-limit pause; it cannot inspect outcome fields or unlock downstream formal-research gates.
- At the 2026-08-24 provider-reset heartbeat, the r2 preflight stopped before digest/source validation because `dataset-download-f914feaddea04e37b3cbdcfce2b0179b` was absent from the workspace SQLite database and from the configured PostgreSQL repository. It is therefore impossible to prove the frozen checkpoint/progress/retry boundary, so no provider request was issued.
- On 2026-08-26 the research owner selected the append-only r3 lineage path. R3 publication remains conditional on metadata-only recovery of the same frozen job/checkpoints; a new source hash or config revision cannot substitute for missing durable checkpoint evidence.
- Metadata-only recovery searched the workspace SQLite database, every other workspace/sibling SQLite candidate, every connectable database/schema on local PostgreSQL listeners 5090, 5432, and 5433, existing worktrees, and running/stopped Docker container inventory. None contains `dataset-download-f914feaddea04e37b3cbdcfce2b0179b`; no partition payload was selected.
- R0, r1, and r2 artifacts remain canonically valid historical evidence, but their aggregate checkpoint digests and counts cannot recreate the missing job request order or 678 compressed partitions. They therefore cannot authorize a physical resume at index 678.
- Current acquisition source files are clean at HEAD `3ff0182c2024fb5419b7ed4f5c5cce8df5ef4813`: provider `33c8619a686f6dbda9568d653139f03acf44059eb598938e03198070a161d574`, downloader `e194610613f9094e8d610a5ccd2f0843ef6ff59dffeaa803e7cfe3913fc7f036`, and CLI `5950bd1d5117f85c2412a316ab46fe83a2f9579a729b82baff4642d885fce675`. The provider differs from r2, so r2 remains non-executable even if a store later reappears until a new reviewed configuration is registered.
- R2's recorded commit does not reproduce the three recorded working-tree source hashes. A future fresh lineage must pin a commit whose Git blobs reproduce every source hash, not merely record the observed HEAD beside uncommitted bytes.
- Because durable recovery failed, publishing a configuration labelled as an r2 resume would fabricate evidence. The next safe action is a separately authorized fresh acquisition job starting from a newly frozen target order; it is not a continuation of the 678-partition job.
- On 2026-08-26 the research owner explicitly authorized creation of that fresh r3 job. The authorization covers new job initialization and lineage freezing, not historical Kbar execution, Price Dataset creation, population freeze, outcome generation, holdout, orders, or subscriptions.
- The existing generic `create_job()` is not safe for this gate: `provider_instruments()` calls `get_market_stocks()`, which reads Shioaji Snapshots and filters by positive price, while the existing CLI immediately calls `run()` and starts Kbar acquisition. Fresh r3 initialization therefore requires a separate metadata-only contract-catalog path.
- The fresh universe identity is `ALL_CURRENT_CONTRACT_CATALOG_V1`, not the prior Snapshot-filtered `ALL_CURRENT`. It must be recorded as a new acquisition lineage and cannot merge observations or checkpoints with r0/r1/r2.
- Job identity must be deterministic from the frozen provider environment, date range, universe policy, and full target-order digest. `create_job_once()` may be used only with exact post-conflict request comparison and zero-partition verification.
- The immutable target manifest must persist every target index, symbol, name, and market. A digest alone is insufficient because the lost r2 database already demonstrated that the mutable job store cannot be the sole target-order authority.
- Fresh job `dataset-download-r3-e9981217a1d36c213e121db3ebaa26e7` was created exactly once in PostgreSQL as `DATASET_DOWNLOAD / QUEUED`, progress 0, no resource, no error, zero partitions, and zero target Dataset rows. The frozen request covers 2023-08-19 through 2026-08-18 and has digest `f04d7e78ba5c79390bf471c53741efbadf061aafd89abe5682654a9594047256`.
- Metadata-only Shioaji contract discovery produced 2,781 unique sorted targets: 1,380 TWSE and 1,401 TPEX. This is intentionally a new `ALL_CURRENT_CONTRACT_CATALOG_V1` acquisition universe; it is not the old 2,738 Snapshot-filtered target set and is not a formal PIT/ordinary-equity denominator.
- The full immutable target order has digest `470032790f69f25a98a61d13686598776dcc1d8819379427113ebcc44305331e`; target-manifest digest is `61dc598c47e3168f67c36862606d12b3bad2fce709e58d1a7543e663c0453827`.
- Fresh r3 configuration digest is `a970a91d22501f4ec670d2c3e40e7ac71c9405ce9031c881bb639034f8c29c5b`. Its status is `FROZEN_JOB_CREATED_SCAN_NOT_AUTHORIZED`; historical Kbar, Dataset materialization, formal coverage audit, population freeze, outcome, holdout, order, and trade-subscription locks all remain false.
- Shioaji initialization used `1.7.2 / simulation=true / subscribe_trade=false`. The SDK's observed `APISUB/.../CONTRACT/TW/STK` event was automatic contract-catalog loading, not Tick/BidAsk, broker-account, or order subscription. The initializer made no Snapshot, usage, or Kbar call.
- Focused fresh-r3 and predecessor artifact gates pass 18 tests. Full no-DSN regression passes 1,501 tests with 57 skips. Compilation and `git diff --check` pass; Ruff remains unavailable and is not claimed.
- The only valid recovery paths are restoring the immutable store containing the original frozen job and checkpoints, or creating a new acquisition lineage after explicit research-owner approval. Recreating the job or replaying from index 678 would silently break r0/r1/r2 lineage and is prohibited.
- A pause request to the app-owned heartbeat configuration timed out without persisting a state change. Its current prompt remains fail-closed at the missing-job preflight, and its configuration file must not be edited as a workaround.
- Independent review found the first fresh-r3 execution boundary was unsafe: the job was still `DATASET_DOWNLOAD / QUEUED`, so generic `--resume` could ignore the artifact-only false flag. A metadata-only compare-and-set quarantined the exact untouched job as `PRICE_COVERAGE_PREPARED / PREPARED`; request digest, zero partitions, and zero Dataset remained unchanged.
- Generic history resume now rejects `FRESH_R3_NO_CHECKPOINT_REUSE` in the downloader and performs the same metadata-only check before CLI provider construction. No dedicated activation runner exists, so the prepared job remains non-executable.
- The r3 initializer now uses a fixed-root, no-follow directory-descriptor artifact store. A caller-owned 0600 `flock` spans initialization; JSON/sidecar publication uses staged fsynced files, sidecar-first hard links, directory fsync, and exact one-sided replay. Symlinked roots, JSON, sidecars, and concurrent lock attempts fail closed in focused tests.
- The original r3 config digest `a970a91d22501f4ec670d2c3e40e7ac71c9405ce9031c881bb639034f8c29c5b` remains unchanged. Append-only quarantine rev2 digest `f44d1e87a81fd9aedded1d7bc1a42e2032d564e437431fe47f5d62fcf3db27af` explicitly records that initial initializer bytes and historical lock/symlink properties are not retroactively proven; every execution/research permission remains false.
- Post-remediation verification passes 59 focused tests and the complete suite with 1,537 passed / 59 skipped. Compile, canonical sidecars, and `git diff --check` pass; Ruff is unavailable and was not executed.
- Phase 27 inspection found that `ResumableHistoricalDownloader.run()` always decodes partition payloads and materializes a READY Dataset after acquisition. The dedicated r3 path must reuse only the checkpointed acquisition loop and terminate as `SCAN_COMPLETE` with `resource_id=None`; it must never call partition payload iteration, Dataset materialization, or `upsert_dataset`.
- The mutable job row currently has no activation binding field and `update_job()` cannot change `kind`. Phase 27 therefore needs a narrow compare-and-set repository operation that binds the dedicated scan kind to one canonical activation digest and preserves that digest in every mutable scan progress message for exact resume verification.
- Phase 27 now implements that boundary without publishing activation authority: a fixed-name activation artifact must pin exact target/quarantine/request/order/source identities; every resume must present its canonical digest; the dedicated runner holds the acquisition lock through provider and repository close; rate-limit pauses preserve the binding marker; successful completion produces metadata-only Raw Inventory and keeps Dataset/formal-audit/population/outcome/holdout gates false.
- Activation registration is intentionally deterministic (`--authorized-at` and `--authorized-by` are explicit) and refuses dirty/untracked pinned source paths. The current Phase 26/27 source set is still uncommitted, so no activation artifact can be truthfully published until a scoped Git commit is explicitly authorized.
- Owner continuation authorized the scoped freeze. Phase 26/27 source is committed as `f751843a270c074706c838cb609330716fe12757`; activation source entries reconstruct from that commit and current working bytes still match every pinned content digest.
- Immutable activation artifact `price_coverage_scan_activation_v1_2026-08-26-r3.json` has canonical digest `ce5ecd701915309664320832df89201666083d0e5147488139a2048055e50c4f` and is committed with its sidecar/drift test as `2697619`. Registration and exact replay both reported `provider_built=false`, `historical_kbar_requests_issued=false`, `job_mutated=false`, Dataset disabled, and outcome disabled.
- The activation artifact authorizes only the dedicated historical Kbar raw-coverage scan. It does not itself start the scan and does not unlock Dataset materialization, formal Coverage Audit, population freeze, outcomes, holdout, orders, or trade subscriptions.

## Decisions

- Keep formal evaluation evidence separate from runtime actionability.
- Research owner approved the proposed baseline bundle on 2026-08-20; those values may now be frozen without reinterpretation.
- Use a research JSON artifact plus canonical SHA-256 sidecar and drift tests; do not add another runtime or evaluator abstraction.
- On 2026-08-27 the PM continuation explicitly superseded the pending Shioaji r3 start path for the MVP. Do not construct Shioaji or issue provider, broker, account, subscription, Kbar, order, outcome, or holdout calls. Reuse the upstream immutable FinMind Dataset and existing Dataset/Catalog/Audit seams instead of building a second three-year history.
- The current phase is consumer-side and offline-only. It may inspect source and metadata contracts, but must not materialize a placeholder Dataset or freeze an evaluation universe before the exact upstream FinMind Dataset identity is available.
- Session catch-up found extensive concurrent work, including active changes to `backtest/finmind_snapshot.py` and the upstream FinMind acquisition plan. Treat all of those bytes as user/concurrent work: inspect current source, make no source-code edits, and limit this phase's writes to the existing PR-008 workpad.
- Current source already has an immutable FinMind materialization seam: `HistoricalDatasetCatalog.create_finmind_snapshot_dataset(...)`, `scripts/materialize_finmind_backtest_dataset.py`, verified manifest loading, Dataset binding, and focused FinMind Dataset tests. A second price-history implementation is unnecessary.
- `tests/test_pr008_finmind_pit_price_integration.py` freezes the older FinMind Dataset as `ENGINEERING_REFERENCE_ONLY` and the formal PIT contract as `PREREGISTERED_BLOCKED`; it requires PIT/delisted/reference/corporate-action/calendar/institutional lineage. That formal contract must remain unchanged. The MVP consumer path must be explicitly scoped and must not relabel the new FinMind Dataset as full formal-PR-008 or holdout eligible.
- `CompositeResearchInputManifestV1` already requires digest-pinned protocol, coverage amendment, coverage audit, frozen population, price Dataset, institutional partitions, PIT/classification, calendar, corporate actions, reference data, candidate prior, matched controls, observations, coverage matrix, definitions, code, and exact split ranges. It is the final formal manifest seam, not the first MVP ingestion seam.
- `FinMindSnapshotPlan.identity` already pins `dataset_id`, `source_snapshot_digest`, `plan_identity_digest`, selected complete symbols, partition lineage, reference mapping, counts, source contract, volume/amount semantics, issues, and any `repair_lineage`. The ACTIVE 9960 overlay is included during stream verification and adds `ALTERNATE_SOURCE_REPAIR`; the consumer must preserve that issue and lineage rather than flattening it.
- `HistoricalDatasetCatalog.create_finmind_snapshot_dataset(...)` seals canonical timestamp-major JSONL and a canonical `DatasetManifest`, validates plan/source/payload identity, and always marks this source family `universe_scope=CURRENT_SNAPSHOT`, `universe_selection=FINMIND_COMPLETE_SYMBOLS_V1`, and `research_eligible=false`. Those fields are intentional analytical limitations, not defects to patch for the MVP.
- `materialize_finmind_backtest_dataset.py` already separates plan, immutable Dataset execution, and optional PostgreSQL `ATOMIC_BACKTEST_DEFAULT` activation. PR-008 consumption should verify/load the manifest directly and must not change the global default Dataset binding merely to run the MVP coverage audit.
- The existing `PriceCoverageAuditV1` contract is not source-neutral: it pins the old Shioaji scan configuration and requires final PIT/institutional/calendar/reference/corporate-action lineage. It cannot honestly be reused unchanged for the new FinMind MVP Dataset. The consumer needs an append-only FinMind MVP coverage artifact/revision, while the formal audit contract remains frozen and blocked.
- The repository has reusable PIT universe domain/import/serialization and candidate-prior poison gates, but current production-grade formal PIT/reference evidence remains absent. For the MVP, the evaluation universe must therefore be a narrowly named Dataset-covered current-snapshot universe with `research_eligible=false`; it must not be called a formal PIT population or used for holdout claims.
- The existing FinMind institutional MVP is already deliberately separate from formal PR-008. It publishes a T-day post-close, T+1-usable, provider-neutral candidate batch and keeps outcome/order/production/formal-PIT/holdout permissions false. Reuse that batch identity as the institutional input rather than rebuilding institutional factors from raw FinMind rows.
- Existing architecture already favors `previous-session candidate batch -> CandidatePool/eligible symbol set -> unchanged price strategy`. `StrategyContext` and the backtest entry kernels are price-only, so the MVP consumer should apply institutional membership as an outer arm/filter and pin the batch digest in run evidence instead of adding FinMind fields to strategy core.
- Existing formal `PreviousSessionWatchlistCandidateSource`/Candidate Prior persistence must not ingest the looser MVP artifact under a formal identity. The MVP needs a distinct source/input identity and an offline join by `(target_session, symbol)`.
- FinMind snapshot selection is complete-symbol fail-closed: a symbol is included only when every expected calendar date is represented by `READY` or expected `EMPTY`, with no invalid/extra/missing session or pending repair. `selection_audit` retains excluded symbols and reason dates, while the final Dataset manifest embeds only the included plan identity. Therefore an honest symbol-coverage denominator requires both the immutable Dataset manifest and its saved `FinMindSnapshotPlan`/selection-audit digest; the manifest alone can prove included-session completeness but not acquisition-target coverage.
- ACTIVE source-repair lineage is explicit and sufficient to audit 9960 without reading its bar: case, evidence, review, activation, source, raw/canonical digests, timestamp semantic, volume unit, and contributing job are embedded in the planned partition. Acceptance must require this exact lineage and `ALTERNATE_SOURCE_REPAIR` in Dataset issues.
- The upstream 2026-08-27 repair-aware snapshot plan now exists and is canonically identified: target Dataset `dataset-finmind-sponsor-sha256-4defb3967d4e89f87d920197877358a8237cdf9baa51be1001fb156b70310ce4`, `plan_identity_digest=290d5dc5...d674`, `selection_audit_digest=e058d4fd...faba`, 453 included complete symbols, 1 excluded symbol, 329,331 included symbol-session partitions, 51,213,436 bars, and six disclosed issues including `ALTERNATE_SOURCE_REPAIR`. This is metadata evidence only; no bar payload was read.
- Superseded on 2026-08-27 by the PM-approved handoff: the exact target Dataset manifest now exists and PR-MVP-EVAL-001 pins `manifest_digest=367f81246977646798837d39b4e5bc8a0246877caa7543b3a8b209a753fe02dc` plus `bars_sha256=eadccfdac14d116af25bb089689f23c104a2fdb78ae6ea1d331b8a44a46817dc`. This bounded consumer verification did not reopen or rehash the 10.6 GB bars payload; it relies on the independently approved full equality/payload audit for that digest.
- The plan contains the expected ACTIVE 9960/2026-03-20 lineage (`case=finmind-repair-9f08aa0024440e4601ac`, `review=finmind-repair-review-f28f1fdb50e78806a1df`, `activation=finmind-repair-activation-83ca14d4d3d0ca89ac42`). The later Dataset acceptance test must require the same plan identity and repair lineage.
- No parameterized daily `institutional_mvp_candidate_batch_v1` artifact is currently present in the repository. Only the sealed one-session 2026-08-18 MVP candidate observation/policy exists. A three-year institutional strategy study therefore still lacks a historical institutional candidate series even after the price Dataset is materialized.
- The daily batch service/domain is implemented and semantically strong (T/T+1 binding, exact calendar/source/policy digests, current-mapping counts, expiry, ranks, immutable repository), but no output batch exists yet and `CandidateSource` still has no distinct FinMind MVP value. Thus the provider-to-batch seam exists; the batch-to-CandidatePool/runtime seam remains planned, not implemented.
- For offline MVP research, CandidatePool wiring is not required initially. A provider-neutral evaluator can join immutable batch entries to Dataset sessions/symbols and compare `price_only` versus `price_plus_institutional_filter`, while preserving `research_eligible=false` and avoiding runtime/order integration.
- Existing `BacktestApplication.create_run()` binds only Dataset and Strategy Set; it has no symbol/session membership filter or institutional-batch identity. Running it directly would evaluate every Dataset symbol and could not produce a reproducible institutional arm. The smallest safe consumer seam is a read-only research adapter/view that filters the existing Catalog bar iterator by a digest-pinned `(target_session, symbol)` membership matrix; it must not copy or materialize another price Dataset.
- `ATOMIC_BACKTEST_DEFAULT` activation is unnecessary for this consumer plan and would mutate shared runtime state. The offline MVP evaluator should accept an exact Dataset ID/digest plus candidate-series digest explicitly.
- The formal evaluator can classify a holdout only when a fully research-eligible `CompositeResearchInputManifestV1` exists; a current-snapshot FinMind MVP must remain blocked there. It should reuse formal arm names/metric semantics for comparability, but publish an explicitly non-formal MVP diagnostic artifact rather than fabricate the missing formal manifest fields.
- No production `EvaluationObservation` builder was found on the first bounded search; current observation construction appears test-only. The consumer plan therefore needs one offline, provider-neutral observation-builder seam that pins Dataset, candidate membership, strategy-definition, cost-model, and date-window identities before any outcome calculation.
- Metadata-only coverage profile of the new snapshot plan is internally strong for its declared acquisition subset: 727 sessions; 453/454 complete symbols (99.78%); 329,331/329,331 included symbol-session partitions; 328,535 `READY` and 796 expected `EMPTY` (99.76% non-empty); included market mapping is 353 TWSE and 100 TPEx. The sole exclusion is 7610 with 725 missing sessions plus one invalid partition. These rates use the acquisition-declared 454-symbol denominator, not all Taiwan equities or a PIT universe.
- Size, liquidity, industry, and active-vs-delisted concentration cannot be computed from the FinMind Dataset/plan metadata because its reference mapping contains current symbol/name/market only. This is acceptable as a disclosed MVP limitation but keeps formal coverage/population/holdout gates blocked.
- The only sealed FinMind institutional candidate observation uses source session 2026-08-18 and target session 2026-08-19, while the planned price Dataset ends on 2026-08-18. It therefore has zero target-session overlap and cannot be joined to this Dataset without lookahead or range drift.
- Upstream Dataset identity is not the only missing consumer input. A digest-pinned historical institutional candidate-batch series with target sessions inside 2023-08-19..2026-08-18 is also required; otherwise the MVP universe cannot be frozen and the preregistered 60-session minimum remains `INSUFFICIENT_EVIDENCE`.
- After the approved Dataset handoff, the remaining dependency state is `WAITING_FOR_INSTITUTIONAL_SERIES / INSUFFICIENT_EVIDENCE`. The current authorization ends before coverage-artifact publication, universe freeze, or outcome generation.
- The PM-approved Dataset handoff is now physically present. The Dataset directory, `manifest.json`, `bars.jsonl`, and saved `snapshot-plan.json` are regular, one-link, non-symlink objects. Metadata inspection did not open or iterate `bars.jsonl`.
- The manifest claims the exact approved Dataset ID, internal manifest digest `367f8124...02dc`, bars digest `eadccfda...17dc`, plan identity digest `290d5dc5...d674`, source-snapshot digest `4defb396...10ce4`, 2023-08-19..2026-08-18, 453 requested/observed symbols, 51,213,436 bars, `JSONL_FULL_V1`, `TIMESTAMP_SYMBOL`, `KBAR_1M_V1`, `CURRENT_SNAPSHOT`, and `research_eligible=false`.
- The saved plan independently claims 453 included symbols plus one excluded acquisition target (7610), 329,331 included partitions, 328,535 READY partitions, 796 expected EMPTY partitions, the same six limitations, and exactly one 9960/2026-03-20 READY partition with the approved case/evidence/review/activation and raw/canonical digests. This explains the denominator distinction: Dataset requested/observed is 453 included symbols, while the selection audit evaluated 454 acquisition targets.
- Existing provider-free domain loaders passed: `canonical_registration_manifest()` accepted the exact manifest and canonical bytes; `FinMindSnapshotPlan.from_dict()` verified schema plus plan, handoff, selection-audit, and operation-audit digests; the manifest `plan_identity` equals the saved plan identity exactly.
- PR-MVP-EVAL-001 result is `PASS (METADATA-ONLY)`. The 10.6 GB bars file was only checked as a present regular non-symlink object. Its stated SHA is pinned from the manifest and independently approved PM handoff, not recomputed here.
- The repository still contains only one sealed institutional candidate observation: source 2026-08-18, target 2026-08-19, 17 candidates, all formal/outcome/order/production permissions false. The target is outside a price Dataset ending 2026-08-18, so overlap remains zero and no evaluation universe may be frozen.
- Independent PM review disposition is `PR-MVP-EVAL-001 APPROVE`, P1=0 and P2=0. The reviewer confirmed exact Dataset/plan/9960 pins, no Coverage/Universe materialization, all provider/outcome/holdout/runtime/order boundaries closed, exactly 17 single-session candidates, and exactly zero target-session overlap.
- This approval closes only the Dataset handoff gate. It does not authorize PR-MVP-EVAL-002/003/005 execution, a candidate-series acquisition, a universe freeze, or outcome generation. A separately authorized digest-pinned series with at least 60 overlapping target sessions remains mandatory.
- The owner has now separately authorized the candidate-series acquisition/build gate. The narrow interpretation is FinMind institutional inputs only: fixed source sessions, current mapping, existing three-way-buy/ranking policy, immutable batches, and a digest-pinned series manifest. It does not authorize price reads, Evaluation Universe freeze, outcomes, holdout, runtime/default binding, broker, or orders.
- Local inventory found no published `data/institutional_mvp` daily batches and no candidate-series artifact. The existing seam is single-session: `DailyInstitutionalMvpService` plus `FinMindInstitutionalFlowProvider` and the immutable directory repository.
- The reviewed equity calendar covers all of 2026 and implements `next_trading_day`, so the smallest compatible acquisition window is the latest 60 source/target pairs whose target sessions end no later than the price Dataset's 2026-08-18 boundary. No older multi-year calendar is needed for this minimum gate.
- The existing daily provider issues one usage preflight, one all-market institutional-wide request, and one current StockInfo request per session. A historical series needs a frozen plan and series manifest; it should avoid inventing a parallel candidate rule or formal Candidate Prior identity.
- `FINMIND_API_TOKEN` is absent from the current process environment. The existing daily CLI deliberately loads the project `.env`; credential presence there must be checked without printing its value before any provider call.
- The authorized acquisition plan was frozen before provider access with digest `0f11907b01d713aba768ee62ff8c0b9cf61283ed2b326fdc32efaf177c8569b4`. It contains exactly 60 unique reviewed T/T+1 pairs, from source 2026-05-22 / target 2026-05-25 through source 2026-08-17 / target 2026-08-18.
- The accepted candidate-series manifest is `71723f527c673cd0e6d225f03ee7083ad260fe2005b96401792cdd5fd5ed1931`. It pins 60 exact daily batch artifacts, the approved price Dataset manifest/bars/plan/selection/source-snapshot digests, ordered source/target session digests, and per-batch policy/calendar/source-row lineage.
- An earlier pre-review projection (`315c4de6...11e29e`) remains append-only but is not the accepted consumer pin because it did not duplicate the per-batch policy/calendar/mapping-count projection. Its exact daily batch references were unchanged; the accepted manifest closes that lineage projection gap without another provider call.
- Offline exact reconstruction verified all 60 batch payloads through the existing domain contract. The series contains 1,168 ranked candidate observations across 60 sessions and zero valid empty-candidate batches; no source-not-ready session was converted into an empty success.
- The series artifact records `price_or_kbar_read=false`, `return_or_pnl_read=false`, and `backtest_or_holdout_read=false`. Evaluation Universe freeze, outcome, holdout, runtime strategy binding, and order permissions remain false.
- The acquisition stored raw FinMind response SHA-256 evidence in each batch but did not persist the token, Authorization header, raw price data, returns, PnL, or outcomes. An exact second execution completed as an offline `IDEMPOTENT_REPLAY` with zero provider calls in the tested replay path.
- Focused institutional regression passes 59 tests. The full suite reached 1,749 passes and 88 skips with one unrelated mutable Phase 82 selection-bundle job-row drift failure; the changed candidate-series scope does not touch that job or SQLite bundle verifier.
- Phase 32 authorization closes the dependency gate for the non-formal PR-MVP-EVAL-002/003 artifacts only. The existing plan already names their scope: acquisition-declared 454-symbol Dataset coverage and `FINMIND_DATASET_COVERED_CURRENT_SNAPSHOT_MVP`, not PIT or all-Taiwan-equity coverage.
- The formal amendment's 95%/99% gates remain a separate contract whose complete-session definition also requires PIT eligibility, pre-open institutional availability, qualified price inputs, calendar/reference joins, and resolved corporate-action policy. Unknown size/liquidity/industry/delisted concentration and missing PIT/corporate-action evidence prevent this MVP phase from claiming a formal population freeze.
- Metadata profiling reconciled the upstream Dataset exactly: 454 acquisition targets, 453 included symbols, 727 sessions, 329,331 resolved included partitions, 328,535 READY, 796 expected EMPTY, and one excluded symbol (7610). Current reference mapping covers 353 TWSE and 100 TPEx included symbols.
- Treating READY rather than resolved EMPTY as usable one-minute input, 435 of the 454 acquisition targets meet the frozen >=99% per-symbol session criterion. Their aggregate READY coverage is 316,055/316,245 = 99.9399%; symbol coverage is 435/454 = 95.8150%. The numeric 95%/99% gates pass for this acquisition-declared current-snapshot scope, but formal coverage remains blocked by absent PIT/corporate-action/concentration evidence.
- The accepted institutional series contains 1,168 candidate symbol-session observations across 319 unique symbols and 60 target sessions. Exact join to the Dataset shows 271 observations from 118 symbols absent from the price Dataset. The missing observations are concentrated: TWSE 192/1,043 and TPEx 79/125, making TPEx coverage materially weaker.
- Applying the full-window >=99% READY qualification uniformly excludes 18 Dataset symbols; only 3481 and 4764 appear in this candidate series, removing eight additional observations. The resulting non-formal MVP membership has 889 `(target_session, symbol)` rows, 199 symbols, and all 60 target sessions.
- Candidate-series symbol coverage within the partial price Dataset is only 199/319 = 62.38%, so this MVP universe has substantial selection bias and cannot support an all-market or formal effectiveness claim. The freeze may only represent `FINMIND_DATASET_COVERED_CURRENT_SNAPSHOT_MVP` under the user's explicit MVP authorization.
- Published `FinMindMvpPriceCoverageAuditV1` digest `8c60c80a18b3c4aecdaaaff547231203b54361718d4d8638f1ee400ee1690470` with status `PASS_FOR_NON_FORMAL_MVP_FREEZE_ONLY`. It pins the approved price Dataset, accepted 60-session candidate series, uniform 95%/99% numeric rule, 279 typed candidate exclusions, market concentration, formal limitations, and false outcome/holdout/runtime/order permissions.
- Published `MvpEvaluationUniverseV1` digest `dd1f4f30d7795a3dc4d802f51d23f52f8cd3fa0a17d01f2c57f71713011120e3` with status `FROZEN_NON_FORMAL_MVP`. It contains 889 metadata-only memberships, 199 symbols, and all 60 target sessions; `research_eligible=false` and `formal_pit_eligible=false` remain explicit.
- Exact replay returned `IDEMPOTENT_REPLAY` with the same two digests. Focused institutional regression passed 55 tests; compile and whitespace checks passed. Ruff is not installed and was not executed. Full regression passed 1,754 tests with 88 skips and the same unrelated Phase 82 mutable target-job drift failure.
- Phase 33 review reproduced a blocking artifact-boundary weakness: `publish_content_addressed_json()` and `load_canonical_artifact()` only compare the filename with the stored digest field. They do not recompute the body digest or validate the artifact-id prefix, so a self-consistent filename containing a false digest can be published and loaded outside the higher-level CLI verifier.
- Phase 33 review found a blocking path-read weakness in `_load_exact_batches()`: target/source/digest path components are consumed from the candidate-series payload before exact series verification and without canonical date/digest or no-symlink checks. A rehashed untrusted series could make the CLI read a path outside the expected batch tree before failing later.
- The shared content-addressed publisher also follows symlinked artifact roots/category/destination paths and reports `created=true` after a concurrent exact winner. These violate the append-only/root-contained/idempotent acceptance criteria and require adversarial tests before approval.
- Semantic review identified additional fail-closed invariants to test: excluded acquisition symbols must be unique and disjoint from included symbols; candidate market must match the pinned Dataset reference for any covered symbol; candidate-series batch/session counts must agree before coverage arithmetic; and universe verification must first reconstruct the supplied coverage audit from its pinned dependencies rather than trust a self-digest alone.
- The exact-batch loader currently reimplements a weaker read path even though the project already has an exact digest repository port. Because the existing concrete daily repository has a different construction/lock lifecycle, the smallest Phase 33 repair is to harden the bounded offline loader itself: canonical ISO components, lowercase SHA-256, root containment, no symlink/special file, canonical bytes, exact filename/session fields, and body digest verification before returning bytes.
- The current audit builder records `market` from each candidate row but does not compare it to the Dataset's pinned current reference mapping for covered symbols. This can produce a valid membership with false market-concentration evidence. The reviewed behavior should fail closed on any covered-symbol market mismatch.
- `build_mvp_evaluation_universe()` currently accepts any self-digested coverage artifact with its freeze permission set true. The CLI only supplies freshly reconstructed coverage, but the reusable verifier is weaker than the artifact boundary. Phase 33 should make downstream universe verification reconstruct the coverage audit from the exact price reference, snapshot plan, and candidate series before accepting universe membership.
- Review cycle 2 closed the initial four blockers with 74 focused tests passing. The hardened publisher/loader now recomputes identity, validates schema/category, rejects symlink and external-hardlink paths, serializes publication with an owner-controlled lock, reports one exact creator under concurrency, and recovers only its own digest/UUID stale temporary hardlinks.
- The exact-batch loader now validates canonical dates/digest before path resolution, traverses the root/target/source tree with no-follow directory descriptors, requires owner-controlled single-link regular files, and recomputes candidate-batch body digest/id/session lineage before returning the payload.
- The semantic contract now validates candidate-series observation-only authority, counts, ordered T/T+1 sessions, source/target digests, ranks, entry digests, markets, selection-exclusion uniqueness, and exact coverage reconstruction before universe acceptance. Valid zero-candidate and zero-qualified-symbol inputs produce a `BLOCKED` audit instead of a division-by-zero/invalid evidence error.
- A remaining important lineage gap was identified during cycle-2 review: `_verify_dataset_snapshot_binding()` must also pin the snapshot selection-audit digest and the current-snapshot/research/source/issue semantics carried by the Dataset reference. Otherwise a self-rehashed selection audit with the same counts could alter the denominator while retaining the old Dataset reference.
- That remaining lineage gap is now closed. Dataset/snapshot validation pins the selection-audit digest, source, issues, current-snapshot scope, non-research eligibility, complete-symbol selection, JSONL format, one-minute profile, and timestamp-symbol order. READY partitions require positive bar counts and EMPTY partitions require zero without reading bar payloads.
- Real-artifact replay after remediation preserved both Phase 32 artifact body/file digests exactly. Therefore the fixes harden acceptance and publication boundaries without changing the previously frozen non-formal membership or creating a misleading semantic revision.
- Final review also pins each candidate source session to the immediately preceding Dataset trading session for its target. A self-rehashed T+2/session-order drift is rejected before coverage membership can be formed; the real 60-session series passes without changing either artifact digest.
- Phase 33 final disposition is `APPROVED` for the non-formal MVP Coverage Audit and Evaluation Universe Freeze only: P1=0 and P2=0 in scope. Formal PIT population, outcome, holdout, runtime/default binding, production, and order gates remain false.

## 2026-08-27 - Phase 34 Non-Formal A/B Findings

- The minimum safe seam is an engine-level `entry_eligibility(session, symbol)` predicate. It keeps the price-strategy kernel, cost model, capital, bars, exits, and priority identical while applying the frozen institutional membership only as an outer gate.
- The final plan pins 60 target sessions, 435 audit-qualified price symbols, 889 institutional memberships, 61 context sessions, 26,535 symbol-session partitions, and 4,568,727 selected one-minute bars from the existing immutable Dataset. No second Dataset was materialized.
- Two pre-result plan revisions were preserved as failed evidence. Their post-run validator exposed an existing engine boundary where intraday pending entries could cross a session or fill on the terminal bar without a same-day exit opportunity. The engine now cancels those orders as `UNFILLED_END_OF_SESSION` while preserving explicit `DAILY_NEXT_BAR` behavior.
- The final plan digest is `d5e2908df7e984ef563e5a7af3128bebcb9803eef757c005a6fdcc191222bb66`. The final result digest is `1b62e1e7eaa8887e3c604f927f73a4db2628f33f91e03ac7d947364d077c6a52`; a second full two-arm Catalog replay reproduced the same digest and returned `IDEMPOTENT_REPLAY`.
- Price-only produced 793 closed trades with win rate 0.353089533417, expectancy -5,025.964625 TWD, profit factor 0.511475074474, net PnL -3,985,589.947809 TWD, and max drawdown 0.398738059214.
- Institutional-filter produced 77 closed trades with win rate 0.363636363636, expectancy -6,730.431471 TWD, profit factor 0.508140596040, net PnL -518,243.223261 TWD, and max drawdown 0.053291251351.
- The filter reduced trade count by 716 and retained 9.7099621690% of price-only trades. Its win rate increased 1.054683 percentage points, but expectancy declined 1,704.466846 TWD and profit factor was slightly worse. The lower aggregate loss/drawdown primarily reflects much lower exposure and cannot be treated as evidence of incremental alpha.
- This diagnostic does not support promoting the current three-way-buy candidate prior. It also cannot establish rejection for all Taiwan equities because candidate symbol coverage is 62.38%, TPEx inclusion is materially weaker, reference metadata is current rather than PIT, prices are raw/unadjusted, and the FinMind amount/VWAP path is a proxy.
- Result and plan artifacts are canonical, append-only, 0440, single-link, and carry zero formal holdout/provider/runtime/production/order authority. Forbidden secret-key scan found no token, API key, secret, authorization, password, or account identifier.

## Resources

- `/Users/stevehuang-work/.codex/attachments/f23187ca-947a-4986-abc3-8a723158e670/pasted-text.txt`
