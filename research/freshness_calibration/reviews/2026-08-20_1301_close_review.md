# Freshness close-window review — 2026-08-20 13:01 Asia/Taipei

## Decision

**Status: partial close quote evidence; no threshold candidate.**

The artifact passes collector, lifecycle, and integrity gates, but it has no
low-cohort Tick callback and ends before the regular-session boundary. It may
be retained as close-window cadence evidence, but cannot complete the close
session gate or support a threshold.

## Immutable source and provenance

| Field | Value |
|---|---|
| Artifact | `research/captures/freshness_quote/quote_20260820T130116+0800.json` |
| SHA-256 | `1994292cc3c9868952567283f9dc7b02a8ada8dca03a05e7c131872c8e3aed70` |
| Schema / bytes | `freshness_calibration_quote_v1` / 1,065,876 |
| Capture range | 2026-08-20 13:01:16.636560–13:16:16.987836 +08:00 |
| Cohort manifest | `cohort_manifest_2026-08-20_twse_2026-08-19.json` |
| Callback scope | Tick and BidAsk only; `subscribe_trade=False`; no account, order, CA, or trade-callback API |
| Preflight NTP sample | `+32.587 ms +/- 53.185 ms` from `time.apple.com`; two successes and retained timeout attempts; read-only host-clock provenance |

Schema and digest inspection passed. Every symbol received paired `TIC` and
`QUO` acknowledgement. All 2,092 observations are `CONNECTED/ACTIVE`; callback
errors, callback monotonic regressions, duplicate composite observation keys,
and receipts outside the capture interval are all zero.

## Coverage and observed cadence

| Tier / symbol | BidAsk callbacks | Tick callbacks | Longest observed BidAsk gap | Longest observed Tick gap |
|---|---:|---:|---:|---:|
| high / 2886 | 1,636 | 371 | 6.149 s | 13.693 s |
| mid / 6863 | 71 | 6 | 162.377 s | 319.378 s |
| low / 1530 | 8 | 0 | 283.310 s | N/A (no Tick callback) |

The no-Tick low-cohort result occurred after paired acknowledgement with eight
BidAsk callbacks and `CONNECTED/ACTIVE` state. It is additional support for the
already-reviewed rejection of a Tick-only executable-health model. It does not
state that BidAsk is fresh for any duration.

## Boundary and evidence limitations

1. **The regular-session boundary was not observed.** The artifact ends at
   13:16, before the expected 13:30 market/session transition. It cannot
   determine how market/session state, connection/subscription health, and
   quote silence should be distinguished at or after close.
2. **Coverage is 5/6 required callback groups.** `1530/TICK` had no callback,
   so this artifact is not a complete qualified three-tier close sample. Missing
   market activity is a coverage outcome, not a synthetic stale signal.
3. **Source-clock transport latency remains unvalidated.** Negative raw
   event-to-callback values occur in 1,435/2,092 observations. Read-only NTP
   provenance does not establish the provider/exchange clock relation.
4. **No broker/account metric is represented.** This artifact cannot support
   position, order, accounting, or buying-power freshness.

## Required next evidence

1. On a future completed session, retain the same frozen cohort/collector and
   run a full close-regime sample that covers the 13:30 transition under a
   documented market/session-state protocol.
2. Repeat opening, continuous, and close evidence across dates before any quote
   threshold review.
3. Obtain separately authorized read-only broker/account timing evidence for
   the four non-quote thresholds.
