# Freshness close-window review — 2026-08-25 13:01 Asia/Taipei

## Decision

**Status: partial close quote evidence; no threshold candidate.**

This is a third trading-date early-close artifact under the unchanged frozen
cohort, collector, schema, and quality gate. Its integrity and lifecycle
checks pass, but `1530/TICK` has no callback and the capture ends before the
13:30 session boundary. It is retained as evidence; it cannot freeze a quote
freshness threshold or complete the close/session-boundary gate.

## Immutable source and provenance

| Field | Value |
|---|---|
| Artifact | `research/captures/freshness_quote/quote_20260825T130132+0800.json` |
| SHA-256 | `79e883fe1a1e3027caa085d7955d9ee0d21aad0533deff98975ac8ee28228401` |
| Schema / bytes | `freshness_calibration_quote_v1` / 528,301 |
| Capture range | 2026-08-25 13:01:32.179601–13:16:32.583053 +08:00 |
| Cohort manifest | `cohort_manifest_2026-08-20_twse_2026-08-19.json` (SHA-256 `888938c8aa8282bd9ecad4edc3a15a8c86be4c599e80592274c4e2a803d2b2da`) |
| Callback scope | Tick and BidAsk only; `subscribe_trade=False`; no account, order, CA, or trade-callback API |
| Preflight NTP | Five read-only `time.apple.com` invocations selected successful sources; offsets were approximately +0.105 to +0.115 ms. Individual exchange timeouts were retained. |

The NTP check establishes host-clock provenance only; it does not make the
provider market-event clock comparable to the callback clock.

Schema and digest inspection passed. All three symbols received paired `TIC`
and `QUO` acknowledgement. All 1,037 retained observations are
`CONNECTED/ACTIVE`; callback errors and callback monotonic regressions are
zero.

## Coverage and observed cadence

| Tier / symbol | BidAsk callbacks | Tick callbacks | Longest observed BidAsk gap | Longest observed Tick gap |
|---|---:|---:|---:|---:|
| high / 2886 | 823 | 173 | 12.700 s | 15.010 s |
| mid / 6863 | 34 | 6 | 226.624 s | 573.949 s |
| low / 1530 | 1 | 0 | N/A (one callback) | N/A (no Tick callback) |

The structural quality result is `REVIEW_REQUIRED_PARTIAL_COVERAGE`: 5/6
required callback groups are present and `1530/TICK` is absent. This absence
occurred after paired acknowledgement and active lifecycle state. It is a
coverage fact, not a synthetic stale signal.

## Boundary and evidence limitations

1. The capture ends at 13:16, before the 13:30 session boundary. It is
   early-close evidence, not session-boundary evidence.
2. The low-cohort Tick group is absent, so this is not a complete three-tier
   close sample.
3. Raw event-to-callback clock skew occurs in 962/1,037 observations. It is
   retained for provenance and anomaly review; it is not used as transport SLA
   input.
4. This quote artifact contains no broker/account source, so it cannot support
   position, order, accounting, or buying-power freshness.

## Operational disposition

The initial direct capture continued after the interactive launcher returned
and produced the immutable artifact above. A temporary duplicate-prevention
job was stopped before it wrote an artifact; no second close artifact was
created. The normal daily quote scheduler was restored for future sessions.

## Gate effect

All eight FreshnessPolicyV1 thresholds remain unset. `FreshnessPolicyV1`
remains `BLOCKING_EVIDENCE`; Phase 0 is not complete and Portfolio Phase 1
remains blocked.
